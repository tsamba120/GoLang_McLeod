package main

import "fmt"

var x int
var y string
var z bool

func main() {
	fmt.Println(x)
	fmt.Println(y)
	fmt.Println(z)
}

// What are the default, compiler-assigned values for x, y, z when no values is assigned?
// These are called the "zero values"
